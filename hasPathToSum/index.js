const hasPathToSum = (node, sum) => {
  let collection = [];
  let stack = [];
  let hand = []
  
  stack.push(node.val);

  while(stack.length) {
    let target = stack.shift();
    hand.push(target.)
  }
  
}